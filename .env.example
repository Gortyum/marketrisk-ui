# Copia a frontend/.env (o .env.production) para desarrollo/producción local.
# En Vercel se define en el panel (Settings → Environment Variables) la
# variable VITE_API_BASE apuntando al backend desplegado en Railway.
#
#VITE_API_BASE=https://course-backend.up.railway.app